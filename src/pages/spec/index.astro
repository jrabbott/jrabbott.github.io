---
import SpecPage from '../../components/SpecPage.astro';
import { getEntry, render } from 'astro:content';

const doc = await getEntry('spec', 'index');
if (!doc) throw new Error('Missing spec overview');
const { Content } = await render(doc);
---

<SpecPage doc={doc}>
  <Content />
</SpecPage>
